name: ci for cabal sdist

on: [push]

# thanks https://gist.github.com/mstksg/11f753d891cee5980326a8ea8c865233
jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest]
        plan:
          - { lts: "-11.10" } # ghc-8.2.2
          - { lts: "-12.16" } # ghc-8.4.4
          - { lts: "-14.14" } # ghc-8.6.5
          - { lts: "-nightly" }
          - { lts: "" }
    runs-on: ${{ matrix.os }}
    steps:
      - name: Install OS Packages
        uses: mstksg/get-package@v1
        with:
          apt-get: ${{ matrix.apt-get }}
          brew: ${{ matrix.brew }}
      - uses: actions/checkout@v1

      - name: Setup stack
        uses: mstksg/setup-stack@v1

      - name: Install dependencies
        run: |
          stack build --skip-ghc-check --no-terminal --test --only-dependencies
        env:
          STACK_YAML: stack${{ matrix.plan.lts }}.yaml
      - name: Build
        run: |
          stack test --fast --bench --no-run-benchmarks --haddock --no-haddock-deps
        env:
          STACK_YAML: stack${{ matrix.plan.lts }}.yaml
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v1
  #     - uses: actions/setup-haskell@v1
  #       with:
  #         ghc-version: "8.6.5"
  #         cabal-version: "3.0"
  #     - name: Install
  #       run: |
  #         curl -sSL https://raw.github.com/ndmitchell/hlint/master/misc/run.sh | sh -s .
  #         # cabal update
  #         # cabal install hlint
  #         # cabal install ormolu
  #     - name: lint
  #       run: |
  #         hlint .
  #         # ormolu --mode check $(find . -name '*.hs')
