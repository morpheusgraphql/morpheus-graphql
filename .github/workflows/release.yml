name: automatic Release

# on: 
#   push:
#     tags:
#     - '*'

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: install stack
      run: | 
        stack upgrade
    - name: upload release
      run: | 
        stack upload . -u nalchevanidze -p ${{ secrets.GITHUB_TOKEN }}